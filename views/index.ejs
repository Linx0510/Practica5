<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Управление пользователями</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h1>Список пользователей</h1>
        
        <div class="action">
            <a href="/add-user" class="btn btn-primary"><img style="width: 40px; height: 40px;" title="Добавить пользователя" src="/img/user-add.svg" alt=""></a>
            <a href="/add-tarif" class="btn btn-secondary"><img style="width: 40px; height: 40px;" title="Добавить тариф" src="img/add.svg" alt=""></a>
            <a href="/add-social-status" class="btn btn-secondary"><img style="width: 40px; height: 40px;" title="Добавить соц. статус" src="/img/add.svg" alt=""></a>
        </div>

        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Имя</th>
                    <th>Фамилия</th>
                    <th>Телефон</th>
                    <th>Email</th>
                    <th>Соц. статус</th>
                    <th>Тариф</th>
                    <th>Абон. плата</th>
                    <th>Действия</th>
                </tr>
            </thead>
            <tbody>
                <% users.forEach(user => { %>
                    <tr>
                        <td><%= user.id_user %></td>
                        <td><%= user.first_name %></td>
                        <td><%= user.last_name %></td>
                        <td><%= user.phone %></td>
                        <td><%= user.email || 'Не указан' %></td>
                        <td><%= user.SocialStatus.name %> (<%= user.SocialStatus.discount %>%)</td>
                        <td><%= user.Tarif.name %> - <%= user.Tarif.price %> руб.</td>
                        <td>
                            <% if (user.Apays && user.Apays.length > 0) { %>
                                <%= user.Apays[0].price %> руб.
                            <% } else { %>
                                Не установлена
                            <% } %>
                        </td>
                        <td class="actions">
                            <a href="/edit-user/<%= user.id_user %>" class="btn btn-edit" title="Редактировать пользователя"><img style="width: 40px; height: 40px;" src="/img/draw-edit-new.svg" alt=""></a>
                            <form action="/delete-user/<%= user.id_user %>" method="POST" style="display:inline;">
                                <button type="submit" class="btn btn-delete" title="Удалить пользователя" ><img style="width: 40px; height: 40px;" src="/img/trash.svg" alt=""></button>
                            </form>
                            <a href="/add-apay/<%= user.id_user %>" class="btn btn-secondary" title="Добавить платеж"><img style="width: 60px; height: 60px;" src="/img/add.svg" alt=""></a>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
</body>
</html>